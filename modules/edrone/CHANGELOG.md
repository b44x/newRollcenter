1.1.37
* All .js files move from edrone.php file as text to view/js/* folder
* Change endpoint name form edroneajax.php to edroneAddToCartAjax.php
* New endpoint with user session data - edroneUserSessionData.php
* Stop injecting logged-in users data into the view-source to avoid problems with template caching.
* JS eval function removed.
* Fields trace_url && edrone_js_url removed from admin view.

1.1.38
* coupon field added to serverside order action

1.1.39
* App_Id moved to one function above, to have an access to external.js file. 

1.1.40
* Added checking if cart object exists
* Added checking if product_id exists

1.1.44
* trim of $appId before saving configuration to database

1.1.45
* change version module in edrone.php

1.1.46
* add category_id and category_name to every trace

1.1.51
* added subscription status sync

1.1.52
* added multilang support
* added brand fields
* minify js
* send trace when entering category with category id and name

1.1.53
* remove backward compatibility (Prestashop 1.6.x)
* fix missing data when order is made

1.1.54
* fix add to cart from product list

1.2.0
* Improved the UI of configuration page
* Improved server-side add to cart tracking to avoid sending too little fields
* Improved add to cart event tracking
* Added homepage_view and product_view events to be handled directly after installation of the package
* Added new data to the subscribe traces (when these are available) - name, last name, birth date
* Now when adblock is detected and user makes an order, it will be sent by server.
* Added subscribe events to the registration and order pages
* Added subscribe tag detection
  * Subscribe - default
  * Footer - for newsletter form located in page footer
  * Register - when user signs up during registration
  * Order - when user doesn’t make an account, but signs up during order creation process
* Implemented widget compatibility for order confirmation page
* Improved the order confirmation page data fetching to be more reliable
* Implemented the order event to front office
* Improved the newsletter statuses data gathering - modified the event listener on the page so now only very specific websites will need to implement custom modification in order to gather newsletter data
* Added user_id uid for each events generated by logged in users and for all users that make an order
* Added support for PrestaShop 8
* Removed jQuery library
* Moved non-module classes from edrone.php to separate files
* Added composer autoload

1.2.1
* Patched an issue that prevented users from installing the module on specific PS version (1.7.5.1)
* Fixed JavaScript error that appeared in console when customer had heavily modified product page
* Fixed an issue that prevented the edrone variables from loading when specific conditions were met

1.2.2
* Patched the fatal error on getInfoPageProduct when getting image url

1.2.3
* Fixed incorrect product_counts on server-side order traces